# spell-checker

Walk through what happens when compute('kit', 'kitten') is called. The strings have different lengths, so trace through how the DP table gets built. What does dp[i][j] represent?
How does closestWords() decide the order of its results? If two candidate words have the same edit distance from the input, which one ranks first?
